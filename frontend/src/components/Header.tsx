import React from "react";

const Header: React.FC = () => {
    return (
        <nav className="header">
            <h1 className="header__title">Selective Practice</h1>
        </nav>
    );
};

export default Header;